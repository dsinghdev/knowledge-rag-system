services:
  rag-app:
    build: .
    container_name: incometax-rag-app
    env_file:
      - .env
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - faiss_index:/app/faiss_index
    stdin_open: true
    tty: true

volumes:
  faiss_index:
