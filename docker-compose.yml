services:
  rag-app:
    build: .
    container_name: incometax-rag
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - faiss_index:/app/faiss_index
    stdin_open: true
    tty: true

volumes:
  faiss_index:
